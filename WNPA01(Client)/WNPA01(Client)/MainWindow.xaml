<Window x:Class="WDPA07_Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WDPA07_Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Background="#2D2D30" 
        Closing="Window_Closing"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1C97EA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Menu Style -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Menu HorizontalAlignment="Left" Height="24" VerticalAlignment="Top" Width="800">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Name="ExitButton" Click="ExitBtn_Click"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="About" Name="AboutMenuButton" Click="AboutMenuButton_Click" IsCheckable="True"/>
            </MenuItem>
        </Menu>
        <Label Name="iplabel" Content="This IP Address:" HorizontalAlignment="Left" Margin="84,47,0,0" VerticalAlignment="Top"/>
        <Label Name="portlabel" Content="This Port:" HorizontalAlignment="Left" Margin="84,86,0,0" VerticalAlignment="Top"/>
        <Label Name="sendlabel" Content="Message to send:" HorizontalAlignment="Left" Margin="84,248,0,0" VerticalAlignment="Top"/>
        <TextBox Name="SendBox" HorizontalAlignment="Left" Margin="220,257,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <Button Name="SendBtn" Click="SendBtn_Click" Content="Send Message" HorizontalAlignment="Left" Margin="220,303,0,0" VerticalAlignment="Top"/>
        <Label Name="receivelabel" Content="Message Received:" HorizontalAlignment="Left" Margin="503,61,0,0" VerticalAlignment="Top"/>
        <Label Name ="IpLabel" Content="" HorizontalAlignment="Left" Margin="220,47,0,0" VerticalAlignment="Top"/>
        <Label Name ="PortLabel" Content="" HorizontalAlignment="Left" Margin="220,81,0,0" VerticalAlignment="Top"/>
        <Label Content="Send to IP: " HorizontalAlignment="Left" Margin="84,124,0,0" VerticalAlignment="Top"/>
        <Label Content="Send to Port:" HorizontalAlignment="Left" Margin="84,166,0,0" VerticalAlignment="Top"/>
        <TextBox Name ="SendIPBox" HorizontalAlignment="Left" Margin="220,125,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <TextBox Name="SendPortBox" HorizontalAlignment="Left" Margin="220,173,0,0" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        <ListBox Name="TransactionLog"  Margin="503,89,98,168"/>
    </Grid>
</Window>